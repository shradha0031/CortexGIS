# CortexGIS: Minimal Dependencies
# These are required for core functionality
# All imports are gracefully handled if missing; system works in "stub mode" without them

# Core UI Framework
streamlit>=1.28.0

# JSON & Data Handling
pydantic>=2.0.0

# Optional: Uncomment for Python syntax validation
# pylance>=0.1.0
